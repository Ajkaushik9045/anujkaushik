import 'package:flutter/material.dart';

class AppColors {
  static const Color primaryColor = Color.fromARGB(255, 20, 80, 129);
    static const Color secondaryColor = Colors.white;
    static const Color secondaryBlackColor = Colors.black;
    static const Color textColorWhite= Colors.white;
  static const Color textColorBlack= Colors.black;
}
